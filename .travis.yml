Language: c

Compiler:
  -gcc

before install:

  - sudo apt-get update

install:

  - echo  "Reached Install Section"

script:  

  - echo "Welcome to the Script Section!"
  - gcc main.c -o results -lcunit
  - ./results
  
